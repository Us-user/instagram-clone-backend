namespace Domain.Enums;

/// <summary>
/// Тип объекта, в котором сделано упоминание (@username). Вместе с <c>EntityId</c> задаёт
/// полиморфную ссылку на пост/комментарий/ответ на сторис. Значения фиксированы —
/// новые типы добавлять только в конец, чтобы не сдвинуть уже сохранённые числовые коды.
/// </summary>
public enum MentionEntityType
{
    Post = 0,
    Comment = 1,
    StoryReply = 2
}
